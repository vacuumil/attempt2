import React from 'react';
import { StubContainer, StubTitle, StubText } from '../../shared/StubComponents';

export const SimulatorTab: React.FC = () => {
  return (
    <StubContainer>
      <StubTitle>🎯 Тренажер ILS</StubTitle>
      <StubText>Раздел в разработке. Здесь будет интерактивный тренажер захода на посадку.</StubText>
    </StubContainer>
  );
};