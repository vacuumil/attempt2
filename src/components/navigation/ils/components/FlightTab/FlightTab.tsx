import React from 'react';
import { StubContainer, StubTitle, StubText } from '../../shared/StubComponents';

export const FlightTab: React.FC = () => {
  return (
    <StubContainer>
      <StubTitle>✈️ Полетный режим</StubTitle>
      <StubText>Раздел в разработке. Здесь будет продвинутый режим полета с анализом.</StubText>
    </StubContainer>
  );
};